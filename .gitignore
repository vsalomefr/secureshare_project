# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock

# Build / Vite
dist/
.env

# Docker
*.env
*.log
docker-compose.override.yml
git add .
git commit -m "Initial commit: Full SecureShare project with frontend, backend, Dockerfiles, docker-compose"
